{{! navigation template}}
<nav id="site-nav">
  <div class="Container-Large wrapper">
    <section class="title">
      <h1>{{@blog.title}}</h1>
    </section>
    <section class="nav">
      <ul>
        {{#foreach navigation}}
          <a href="{{url absolute="true"}}"><li>{{label}}</li></a>
        {{/foreach}}
      </ul>
    </section>
  </div>
</nav>
